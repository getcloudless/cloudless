"""
AWS Provider

This module implements support for using AWS as a backing provider.
"""
from butter.providers.aws import (network, subnetwork, instances, paths,
                                  inventory)
